[project]
name = "pytest-homework"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0",
    "pyyaml>=6.0",
    "allure-pytest>=2.13",
]

[dependency-groups]
dev = [
    "pytest-order>=1.5.0",
    "pytest-xdist>=3.5",
]
