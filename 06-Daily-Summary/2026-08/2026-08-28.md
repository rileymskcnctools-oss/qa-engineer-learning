---
tags: [每日总结]
date: 2026-08-28
duration:
status: in_progress
---

# 每日总结 - 2026-08-28

## 学习内容

| 时间 | 内容 | 笔记 |
|------|------|------|
| | 强制等待与隐式等待（等待机制专章：三种等待、隐式等待无法解决的问题、显式等待初级） | [[../../02-Course-Notes/Web自动化测试/Ch07-强制等待与隐式等待\|Web Ch07]] |
| | 常见控件交互方法（元素操作 send_keys/clear/click + 属性获取 .text/get_attribute） | [[../../02-Course-Notes/Web自动化测试/Ch08-常见控件交互方法\|Web Ch08]] |
| | 自动化测试定位策略（七条选型原则 + 特殊定位场景：alert/日期控件/文件上传） | [[../../02-Course-Notes/Web自动化测试/Ch09-自动化测试定位策略\|Web Ch09]] |

> 今天三章串成一条完整链路：**定位（Ch06/Ch09）→ 等待（Ch07）→ 操作/取值（Ch08）→ 断言（Ch04）**。Ch09 在 Ch06"八大方式"之上拔高一层"选型策略"，并补齐 alert/日期/上传三类特殊场景。

---

## 学习任务

### 任务 1：三种等待各写一遍（40 分钟）
在 `02-Course-Notes/Web自动化测试/作业/homework03-selenium-basic/` 新建 `test_waits.py`：
1. 强制等待：`time.sleep(3)` 打开 ceshiren.com，打印耗时 —— 感受"固定 3 秒"
2. 隐式等待：`driver.implicitly_wait(5)` 后 `find_element`，故意找一个慢加载元素，对比有无隐式等待的报错差异
3. 显式等待：`WebDriverWait(driver, 10).until(EC.visibility_of_element_located((By.XPATH, "...")))`，观察元素 0.8 秒就出现时是否立刻返回（不等满 10 秒）

### 任务 2：验证 `time.sleep` 的单位（10 分钟）
跑 `time.sleep(3)` 和 `time.sleep(1)` 各一次，用秒表/打印时间差确认**单位是秒**；对照课程"原理"行写的 `time.sleep(3000)`（标注毫秒），在 Ch07【易错点】里写下你的判断依据。

### 任务 3：复现"元素在 DOM ≠ 可交互"（30 分钟，本课核心）
1. 找一个"存在但被 loading 遮罩/动画挡住"的元素，只用隐式等待去 `click()`，记录报错类型（目标：亲眼看到 `ElementClickInterceptedException` 或 `ElementNotInteractableException`）
2. 改成先 `EC.invisibility_of_element_located(遮罩)` 再 `EC.element_to_be_clickable(按钮)`，确认问题消失
3. 在 Ch07 知识点 5 的【我的理解】写下结论

### 任务 4：`until` 传参踩坑（10 分钟）
故意写 `WebDriverWait(driver, 10).until(driver.find_element(...))`，观察报错；再改成传条件对象跑通。理解"`until` 要可调用条件，不是查找结果"。

### 任务 5（可选）：显式等待自定义条件（15 分钟）
用 lambda 写一个自定义条件（如"等列表条目数量 > 5"），跑通后记录 `until` 接受任意 callable 的用法。

### 任务 6：控件交互四方法跑通（30 分钟，Ch08）
在 `homework03-selenium-basic/` 新建 `test_interactions.py`：
1. 打开 sogou.com：`send_keys` 输入 → `clear` 清空 → 用 `get_attribute("value")` 断言已清空（空串）→ 再输入 → `click` 搜索
2. 打开 vip.ceshiren.com/#/ui_study：分别用 `.text` 和 `get_attribute("name")` 取值并 print，记录差异
3. 故意用 `.text` 取输入框内容，观察是否为空串（验证 Ch08 易错点）

### 任务 7（可选）：复现课程代码 bug（10 分钟）
把 Ch08 课程第一个示例原样写一遍（`def test_interaction_demo(self, driver)` 带 driver 参数），跑 pytest 看报错 `fixture 'driver' not found`，再改成 `self.driver` 跑通。

### 任务 8：特殊定位场景三练（40 分钟，Ch09）
1. **alert**：找/造一个会弹 alert/confirm 的页面（或用 `execute_script("alert('x')")` 触发），用 `switch_to.alert` 取文本 + `accept()`/`dismiss()`，记录不处理 alert 时的报错
2. **日期控件**：找 Element UI 的 `el-date-picker`（readonly），先 `removeAttribute('readonly')` 再 `send_keys`，或 JS 改 value 后 `dispatchEvent(new Event('input'))`
3. **文件上传**：造一个 `<input type="file">`，用 `send_keys(绝对路径)` 上传，验证非 input 型按钮不能这样传

**总计约 2.5~3 小时**，任务 1、3、6、8 必做，任务 2、4 是高频面试陷阱。

---

## 我的理解（最重要）
> 用自己的话回答：
> 1. 三种等待各解决什么问题？为什么"显式为主力、隐式做兜底、强制只调试"？
> 2. "元素在 DOM ≠ 元素可交互"这句话，为什么是理解隐式等待短板、以及为什么需要显式等待的钥匙？
> 3. 一条 UI 用例从定位到断言，完整链路是"定位 → 等待 → 操作 → 取值 → 断言"，中间每一步分别对应哪个知识点？
> 4. 为什么 alert、日期控件、文件上传这三类场景，"常规定位"会失效？它们各自绕开的办法是什么？

---

## 今天没搞懂的问题
-
-
-

## 面试可能怎么问

**Q1**：Selenium 有几种等待方式？区别是什么？项目里怎么组合用？

**Q2**：`time.sleep()` 和 `Thread.sleep()` 有什么区别？单位分别是秒还是毫秒？

**Q3**：隐式等待有什么缺陷？什么情况下它救不了你？（"元素在 DOM ≠ 可交互"）

**Q4**：`.text` 和 `get_attribute` 有什么区别？输入框的值用什么取？（`.text` 取可见文本；输入框用 `get_attribute("value")`）

**Q5**：说说你选择定位器的通用原则？（契约优先 → id/name → 组合 → 动态 xpath → 重复处理 → 父子 → js 兜底）

**Q6**：遇到 alert 弹框怎么处理？为什么 find_element 找不到它？（原生对话框 → switch_to.alert）

**Q7**：文件上传怎么自动化？（input 型 send_keys 绝对路径，绕过系统对话框）

---

## 企业真实场景

1. **夜间构建 flaky 治理**：6 条用例偶发 `ElementClickInterceptedException`，排查是 loading 遮罩——隐式等待认为"按钮已在 DOM"就放行。修法：先等遮罩消失再等可点击，偶发失败归零。
2. **框架基线配置**：`conftest.py` 统一给 driver fixture 加 `implicitly_wait(5)` 兜底 + 关键交互全用显式等待，是团队自动化稳定性的基线。
3. **review 新人代码**：满屏 `time.sleep(2)`，算一笔账（100 条用例 × 平均多等 3 秒 = 多跑 5 分钟）让人信服，再带改一条用数据说话。
4. **clear() 失效的坑**：`type=number`、日期选择器、第三方 autocomplete 组件的值存在组件内部状态，`clear()` 清不掉 → 兜底用 `Keys.CONTROL+"a"` + `Keys.DELETE` 或 JS 直接改。
5. **定位契约化**：推动研发给关键元素加 `data-test-id`，把定位变成前后端约定——改版只改 PO 层一处，是治本手段。

---

## 收获

1. **三种等待的本质一句话**：强制"死等时间"、隐式"全局找元素"、显式"精确等条件"——组合口诀"显式为主力、隐式做兜底、强制只调试"。
2. **"元素在 DOM ≠ 元素可交互"是本模块最高频考点**：HTML 决定存在、CSS/JS 决定交互、加载分层异步，所以隐式等待只解决"找不到"、解决不了"点不了"。
3. **两个单位陷阱**：Python `time.sleep` 是秒、Java `Thread.sleep` 是毫秒；`until` 要传条件对象而非查找结果——都是面试爱挖的小坑。
4. **控件交互是"基本动作库"**：`click`/`send_keys`/`clear` 服务于流程步骤，`.text`/`get_attribute` 服务于断言调试——缺了任何一环，用例都写不出来。
5. **定位策略 = 方法 + 规则**：会写八种方式不等于会选；七条选型原则 + alert/日期/上传三类特殊场景，是定位进阶的关键。特殊场景的共同点是"元素不在常规 DOM 交互模型里"，要绕开（switch_to.alert / JS 注入 / send_keys 路径）。

---

## 明日

- [ ] 跑完任务 1、3，把"元素在 DOM ≠ 可交互"的报错亲手复现一次
- [ ] 跑完任务 6、8：控件交互四方法 + 特殊定位三练（alert/日期/上传）
- [ ] 把三种等待对比表、七条选型原则各口述一遍（不看笔记）
- [ ] 预习方向：**frame / iframe / 多窗口切换**（稳定性章节下一块；alert 已在 Ch09 处理，接下来是 frame 和多窗口）

---

## 反思
> 今天三章（等待机制 + 控件交互 + 定位策略）合起来，是从"会定位"到"能写一条完整且稳定的 UI 用例"的关键跳板。这三章有一个共同点：**"一看就懂、一写就错"**——`clear()` 清不掉、`.text` 取输入框是空串、`time.sleep` 单位搞反、`until` 传错参数、alert 用 find_element 找不到、日期控件改 value 不触发绑定。每一个坑都必须亲手踩一次才会刻进脑子。别停留在"我会了"的错觉，务必把任务 3（复现 ElementClickInterceptedException）、任务 6（四方法跑通）、任务 8（特殊场景三练）亲手跑出来——那是今天最值钱的三个可验证结果。
